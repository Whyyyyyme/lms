@extends('layouts.app', ['title' => 'Kelola Absensi'])
@section('content')
@include('partials.page-header', ['eyebrow' => 'Asisten', 'title' => 'Kelola Absensi', 'description' => $attendance->kelas?->course?->name.' - '.$attendance->kelas?->name])
<div class="mb-5 flex flex-wrap gap-3">@if($attendance->is_open)<form action="{{ route('assistant.attendances.close', $attendance) }}" method="POST">@csrf @method('PATCH')<button class="rounded-2xl bg-amber-600 px-4 py-2 text-sm font-semibold text-white">Tutup Absensi</button></form>@else<form action="{{ route('assistant.attendances.open', $attendance) }}" method="POST">@csrf @method('PATCH')<button class="rounded-2xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Buka Absensi</button></form>@endif @include('partials.delete-button', ['action' => route('assistant.attendances.destroy', $attendance), 'confirm' => 'Hapus sesi absensi ini?'])</div>
<div class="overflow-hidden rounded-3xl border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Mahasiswa</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Check-in</th><th class="px-5 py-3">Ubah</th></tr></thead><tbody class="divide-y">@forelse($attendance->records as $record)<tr><td class="px-5 py-4">{{ $record->student?->name }}<br><span class="text-slate-500">{{ $record->student?->nim_nip }}</span></td><td class="px-5 py-4">@include('partials.badge', ['slot' => $record->status])</td><td class="px-5 py-4">{{ $record->checked_at?->format('d M Y H:i') ?? '-' }}</td><td class="px-5 py-4"><form action="{{ route('assistant.attendances.records.update', [$attendance, $record]) }}" method="POST" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="rounded-xl border-slate-300 text-sm"><option value="hadir" @selected($record->status === 'hadir')>Hadir</option><option value="izin" @selected($record->status === 'izin')>Izin</option><option value="alpha" @selected($record->status === 'alpha')>Alpha</option></select><button class="rounded-xl bg-slate-900 px-3 py-2 text-sm font-semibold text-white">Simpan</button></form></td></tr>@empty<tr><td colspan="4" class="px-5 py-10">@include('partials.empty-state')</td></tr>@endforelse</tbody></table></div>
@endsection
