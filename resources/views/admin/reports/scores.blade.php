@extends('layouts.app', ['title' => 'Laporan Nilai'])
@section('content')
@include('partials.page-header', ['eyebrow' => 'Laporan', 'title' => 'Laporan Nilai', 'action' => '<a href="'.route('admin.reports.scores.export').'" class="rounded-2xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Export CSV</a>'])
<form method="GET" class="mb-5 flex gap-3 rounded-3xl border bg-white p-4"><select name="course_id" class="rounded-2xl border-slate-300"><option value="">Semua matakuliah</option>@foreach($courses as $course)<option value="{{ $course->id }}" @selected((string) request('course_id') === (string) $course->id)>{{ $course->name }}</option>@endforeach</select><button class="rounded-2xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Filter</button></form>
<div class="overflow-hidden rounded-3xl border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Mahasiswa</th><th class="px-5 py-3">Tugas</th><th class="px-5 py-3">Kelas</th><th class="px-5 py-3">Nilai</th><th class="px-5 py-3">Feedback</th></tr></thead><tbody class="divide-y">@forelse($submissions as $submission)<tr><td class="px-5 py-4">{{ $submission->student?->name }}<br><span class="text-slate-500">{{ $submission->student?->nim_nip }}</span></td><td class="px-5 py-4">{{ $submission->assignment?->title }}</td><td class="px-5 py-4">{{ $submission->assignment?->kelas?->course?->name }}<br><span class="text-slate-500">{{ $submission->assignment?->kelas?->name }}</span></td><td class="px-5 py-4 font-semibold">{{ $submission->score ?? '-' }}</td><td class="px-5 py-4">{{ $submission->feedback ?? '-' }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-10">@include('partials.empty-state')</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $submissions->links() }}</div>
@endsection
