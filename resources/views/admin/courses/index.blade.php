@extends('layouts.app', ['title' => 'Matakuliah'])
@section('content')
@include('partials.page-header', ['eyebrow' => 'Admin', 'title' => 'Matakuliah', 'description' => 'Kelola matakuliah praktikum.', 'action' => '<a href="'.route('admin.matakuliah.create').'" class="rounded-2xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Tambah Matakuliah</a>'])
<form method="GET" class="mb-5 flex gap-3 rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><input name="search" value="{{ request('search') }}" placeholder="Cari matakuliah/kode" class="flex-1 rounded-2xl border-slate-300"><button class="rounded-2xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Cari</button></form>
<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr><th class="px-5 py-3">Matakuliah</th><th class="px-5 py-3">Tahun</th><th class="px-5 py-3">SKS</th><th class="px-5 py-3">Kelas</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">
@forelse ($courses as $course)<tr><td class="px-5 py-4"><p class="font-semibold">{{ $course->name }}</p><p class="text-slate-500">{{ $course->code }}</p></td><td class="px-5 py-4">{{ $course->academicYear?->year }} {{ $course->academicYear?->semester }}</td><td class="px-5 py-4">{{ $course->sks }}</td><td class="px-5 py-4">{{ $course->classes_count }}</td><td class="px-5 py-4">@include('partials.badge', ['slot' => $course->is_active ? 'aktif' : 'nonaktif'])</td><td class="px-5 py-4 text-right"><a class="font-semibold text-indigo-600" href="{{ route('admin.matakuliah.show', $course) }}">Detail</a><a class="ml-3 font-semibold text-slate-600" href="{{ route('admin.matakuliah.edit', $course) }}">Edit</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10">@include('partials.empty-state')</td></tr>@endforelse
</tbody></table></div><div class="mt-5">{{ $courses->links() }}</div>
@endsection
