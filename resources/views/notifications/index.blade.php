@extends('layouts.app', ['title' => 'Notifikasi'])
@section('content')
@include('partials.page-header', ['eyebrow' => 'Notifikasi', 'title' => 'Pusat Notifikasi', 'action' => '<form action="'.route('notifications.read-all').'" method="POST"><input type="hidden" name="_token" value="'.csrf_token().'"><input type="hidden" name="_method" value="PATCH"><button class="rounded-2xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Tandai Semua Dibaca</button></form>'])
<div class="space-y-3">@forelse($notifications as $notification)<div class="rounded-3xl border bg-white p-5 shadow-sm {{ $notification->read_at ? '' : 'ring-2 ring-indigo-100' }}"><div class="flex flex-col justify-between gap-3 md:flex-row"><div><p class="font-bold">{{ $notification->title ?? $notification->type }}</p><p class="mt-1 text-sm text-slate-600">{{ $notification->message }}</p><p class="mt-2 text-xs text-slate-400">{{ $notification->created_at?->format('d M Y H:i') }}</p></div><div class="flex gap-2">@unless($notification->read_at)<form action="{{ route('notifications.read', $notification) }}" method="POST">@csrf @method('PATCH')<button class="rounded-xl bg-emerald-50 px-3 py-2 text-sm font-semibold text-emerald-700">Dibaca</button></form>@endunless<form action="{{ route('notifications.destroy', $notification) }}" method="POST">@csrf @method('DELETE')<button class="rounded-xl bg-red-50 px-3 py-2 text-sm font-semibold text-red-700">Hapus</button></form></div></div></div>@empty @include('partials.empty-state', ['title' => 'Tidak ada notifikasi']) @endforelse</div><div class="mt-5">{{ $notifications->links() }}</div>
@endsection
