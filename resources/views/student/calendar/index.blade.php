@extends('layouts.app', ['title' => 'Kalender Akademik'])
@section('content')
@include('partials.page-header', ['eyebrow' => 'Mahasiswa', 'title' => 'Kalender Akademik', 'description' => $activeAcademicYear ? $activeAcademicYear->year.' - '.ucfirst($activeAcademicYear->semester) : 'Tahun akademik aktif belum diatur'])
<div class="grid gap-6 lg:grid-cols-2"><section class="rounded-3xl border bg-white p-6 shadow-sm"><h2 class="font-bold">Deadline Tugas</h2><div class="mt-4 space-y-3">@forelse($assignments as $assignment)<div class="rounded-2xl bg-slate-50 p-4"><p class="font-semibold">{{ $assignment->title }}</p><p class="text-sm text-slate-500">{{ $assignment->kelas?->course?->name }} · {{ $assignment->deadline?->format('d M Y H:i') }}</p></div>@empty @include('partials.empty-state', ['title' => 'Tidak ada tugas']) @endforelse</div></section><section class="rounded-3xl border bg-white p-6 shadow-sm"><h2 class="font-bold">Sesi Absensi</h2><div class="mt-4 space-y-3">@forelse($attendances as $attendance)<div class="rounded-2xl bg-slate-50 p-4"><p class="font-semibold">{{ $attendance->kelas?->course?->name }} - {{ $attendance->kelas?->name }}</p><p class="text-sm text-slate-500">{{ $attendance->session_date?->format('d M Y') }}</p></div>@empty @include('partials.empty-state', ['title' => 'Tidak ada absensi']) @endforelse</div></section></div>
@endsection
