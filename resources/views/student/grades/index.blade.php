@extends('layouts.app', ['title' => 'Nilai Saya'])
@section('content')
@include('partials.page-header', ['eyebrow' => 'Mahasiswa', 'title' => 'Nilai Saya', 'description' => 'Rata-rata nilai: '.number_format((float) $averageScore, 2)])
<div class="overflow-hidden rounded-3xl border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Tugas</th><th class="px-5 py-3">Kelas</th><th class="px-5 py-3">Nilai</th><th class="px-5 py-3">Feedback</th></tr></thead><tbody class="divide-y">@forelse($submissions as $submission)<tr><td class="px-5 py-4 font-semibold">{{ $submission->assignment?->title }}</td><td class="px-5 py-4">{{ $submission->assignment?->kelas?->course?->name }} - {{ $submission->assignment?->kelas?->name }}</td><td class="px-5 py-4">{{ $submission->score ?? 'Belum dinilai' }}</td><td class="px-5 py-4">{{ $submission->feedback ?? '-' }}</td></tr>@empty<tr><td colspan="4" class="px-5 py-10">@include('partials.empty-state', ['title' => 'Belum ada nilai'])</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $submissions->links() }}</div>
@endsection
